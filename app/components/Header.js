import React, { } from 'react';
import { Header, Title } from 'native-base';

export default function HeaderApp() {

    return (
        <Header>
            <Title>
                Let's Play Sudoku
      </Title>
        </Header>
    )
}